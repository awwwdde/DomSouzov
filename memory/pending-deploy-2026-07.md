---
name: pending-deploy-2026-07
description: Незакоммиченные правки сессии 2026-07-06 и что нужно задеплоить
metadata:
  type: project
---

Сессия 2026-07-06: сделан ряд правок, **пока НЕ закоммичены** (рабочее дерево грязное). Проверить `git status` перед продолжением; при коммите — не забыть.

**Фронтенд (достаточно редеплоя фронта):**
- `HeroVideo.tsx` — надёжный автоплей на iOS: `preload="metadata"` (НЕ auto — auto ломал iPhone на тяжёлом файле), muted-атрибут через ref, eager `play()` + `loadeddata/canplay`, IntersectionObserver, фолбэк по касанию. См. [[hero-video-ios-autoplay]].
- Расписание мультидат в [EventDetail.tsx](frontend/src/pages/EventDetail.tsx) — **кубики дат** (зелёные плитки), пропуски дат (напр. 1 января) выпадают сами, без скролла; если времена у дней разные — группировка по набору времён. Плюс `lg:items-start` на сетке (убрал белый прямоугольник под фото).
- Кнопки «Купить билет»/«Вся афиша» одинаковой ширины (`w-full`).
- Вставка фото в текст новостей/афиши (`![](url)`) — рамку у картинок убрал.
- Скрытие прошедших мероприятий: фильтр `filterUpcomingEvents` в [SiteContext.tsx](frontend/src/context/SiteContext.tsx) (прячет на след. день после последней даты; бэк не трогаем).

**Бэкенд (требует пересборки/редеплоя бэка через Docker!):**
- [backend/main.py](backend/main.py) — `index.html` теперь отдаётся с `Cache-Control: no-cache, must-revalidate`. Причина: Safari на iPhone держал старую оболочку → старый бандл (баг «запушил, а на телефоне по-старому»). **Без редеплоя бэка не подействует**, и на телефоне нужен разовый сброс кэша Safari.

**Про hero-видео:** ffmpeg установлен (winget Gyan.FFmpeg), боевое видео уже пережато и залито (~19 МБ, `/uploads/0c90397f…mp4`). Команда пережатия — [[video-compress-command]]. Постер (`hero_video_poster`) в настройках всё ещё пуст — стоит задать.

Следующая большая задача — [[halls-equipment-feature]].
